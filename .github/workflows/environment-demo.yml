name: Environment Scope Demo

on:
  workflow_dispatch:
  push:

env:
  COMPANY: GoLearnam

jobs:
  demo:
    runs-on: ubuntu-latest

    env:
      TEAM: Frontend

    steps:
      - name: Step 1
        env:
          DEVELOPER: Melz
        run: |
          echo "Company: $COMPANY"
          echo "Team: $TEAM"
          echo "Developer: $DEVELOPER"

      - name: Step 2
        run: |
          echo "Company: $COMPANY"
          echo "Team: $TEAM"
          echo "Developer: $DEVELOPER"