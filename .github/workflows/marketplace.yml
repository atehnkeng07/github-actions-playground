name: Marketplace Demo

on:
  workflow_dispatch:

jobs:
  demo:
    runs-on: ubuntu-latest

    steps:
      - name: Download my repository
        uses: actions/checkout@v4

      - name: Show the files in my repository
        run: ls